import 'package:flutter/material.dart';

class {{init_feature.pascalCase()}}_Screen extends StatelessWidget {
  const {{init_feature.pascalCase()}}_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}